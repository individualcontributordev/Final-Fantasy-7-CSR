# CSR-patched disc images (gitignored)

e.g. `FINALFANTASY7_D1 (patched).bin` (+ `.cue`). Optional `.m3u` playlists may be committed.

Diff against `workspace/pristine/` when building builder layers (quote paths with spaces).
