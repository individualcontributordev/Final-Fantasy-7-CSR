# Task: (idle)

No active Windows task. Last ship: csr-plus-scene-aerith-house-v0.1.1 (Elmyra on entry).

Layout reminder:

    pristine/                 retail (store once)
    builder zip .bin          session edit
    builder/                  published layers
    cache/                    optional base reconstructs
    workspace/                legacy fallback only

## Copy-paste

    cd "$(git rev-parse --show-toplevel)"
    git pull --ff-only

## Evidence

    (none)
