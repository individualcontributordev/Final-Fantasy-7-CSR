# Task: (idle)

No active Windows task. The Mac agent overwrites this file, commits, and pushes before each Windows run.

## Copy-paste

```bash
cd "$(git rev-parse --show-toplevel)"
git pull --ff-only
```

## Evidence

```
```
